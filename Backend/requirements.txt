\
langchain-text-splitters
mem0ai
# Additional dependencies might be needed from langchain_arch/requirements.txt?
# Let's assume the needed langchain deps are covered here.
# We need an HTTP client. Let's add httpx.
httpx>=0.27.0 # For making async requests from tools

# E2B SDK
e2b-code-interpreter

# FastAPI (if not managed elsewhere)
fastapi
uvicorn[standard]

# Python libraries for the E2B sandbox script (example, installed in sandbox)
# pandas
# matplotlib
# seaborn
# plotly

# --- Other internal packages --- #
# Assuming langchain_arch might be installed as editable or handled via PYTHONPATH
